namespace ProjectGenerator.Core.Enums;

// Specifies the type of pages a project can have.
public enum PageType
{
    SinglePage = 0, // A single-page project
    MultiPage = 1   // A multi-page project
}